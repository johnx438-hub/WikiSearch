# turbovecWikiTest01 — 依赖清单

turbovec>=0.3.0
whoosh>=2.7.4
numpy>=1.24.0
jieba>=0.42.0
pyyaml>=6.0

# Phase 4: Office+PDF 转换管道
python-docx>=1.1.0          # DOCX 解析
pymupdf>=1.23.0             # PDF 解析（PyMuPDF）
pymupdf4llm>=0.0.17         # PyMuPDF4LLM（可选，用于高质量 PDF→Markdown）

# Embedding模型通过 Ollama 本地服务提供（qwen3-embedding:8b）
# ollama pull qwen3-embedding:8b
